import React from "react";
import PizzaFormName from "./pizzaForm/pizzaFormName";

const User = {
    name: "",
    order: "{order}",
    orderParam: "{orderParam}", // all parameters should apply together
    price: "{price}", // Return price summed of order and parameters
  };

export default User
